name: CI
on:
  push:
    branches: [main, "task/**"]
  pull_request:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: make install
      - run: make lint
      - run: make typecheck
      - run: make layers
      - run: make test
      - run: make spec-check
      - run: make spec-diff
      - run: make contract
      - run: make security
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: make postman
      - run: make load
      - run: make docker
      # Task 3: the data layer against a throwaway PostgreSQL (compose, generated passwords).
      - run: make env
      - run: make db-gate
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: reports
          path: |
            coverage.xml
            /tmp/devdash-load_stats.csv
            /tmp/devdash-load-sql_stats.csv
