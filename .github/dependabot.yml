version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    # This repository manages dependencies with uv (pyproject.toml + uv.lock).
    # Dependabot's pip ecosystem reads pyproject.toml for version constraints.
    open-pull-requests-limit: 10

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
