databases:
  - name: ih-task3-db
    plan: free
    databaseName: ih_task3
    user: ih_task3

services:
  - type: web
    name: ih-task3-api
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && alembic upgrade head"
    startCommand: "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: info
      # Provisioned and injected by Render from the database resource
      # above — never typed in by hand, never committed.
      - key: DATABASE_URL
        fromDatabase:
          name: ih-task3-db
          property: connectionString
      # Comma-separated allowed browser origins. Set in the Render
      # dashboard (not committed) to wherever a frontend will call this
      # from.
      - key: CORS_ORIGINS
        sync: false
