services:
  - type: web
    name: ih-task3-api
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && alembic upgrade head"
    startCommand: "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: info
      # No database resource here on purpose: Render allows one free
      # Postgres per workspace and this project's other deployment (Task 4)
      # uses it. Provide a connection string from any external Postgres
      # (e.g. Neon) in the Render dashboard — never commit it.
      # `sync: false` means "this value lives in the dashboard".
      - key: DATABASE_URL
        sync: false
      # Comma-separated allowed browser origins.
      - key: CORS_ORIGINS
        sync: false
