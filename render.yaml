services:
  - type: web
    name: ih-task2-api
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      # Demo deployment (ADR-221): the pack refuses seeding when APP_ENV=production, and
      # this in-memory service has no other way to create the first lead. Switch to
      # production and drop SEED_PROFILE when real persistence arrives in Task 3.
      - key: APP_ENV
        value: development
      - key: LOG_LEVEL
        value: info
      - key: DOCS_ENABLED
        value: "true"
      - key: SEED_PROFILE
        value: default
      # In-process storage: one worker only, and data resets on every restart.
      - key: WEB_CONCURRENCY
        value: "1"
      - key: SECRET_KEY
        generateValue: true
      - key: SEED_PASSWORD
        sync: false
      # Comma-separated browser origins (no wildcard). Set in the Render dashboard.
      - key: CORS_ORIGINS
        sync: false
